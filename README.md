# OmniNews feed

Signed MN1 bulletins served as `feed.json` for the OmniNews / MeshNews / BitNews apps.
Generated by the MeshNews gateway (`gateway.py --publish-json`). Each bulletin is
Ed25519-signed; the apps verify before display or relay.

Update by re-running the gateway and pushing the new feed.json.
